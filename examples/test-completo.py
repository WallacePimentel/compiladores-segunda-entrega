if __name__ == "__main__":
    def iniciar():
        return print("Iniciando o programa...")
    x = 10
    y = 20
    def testa_algo():
        return True
    def processa_resultado(val):
        return "Processado com sucesso!"
    limite = 100
    limite = 200
    (10 + 20 + limite)
    iniciar()
    ("maior" if (limite > 50) else "menor")
    (10 if True else False)
    def _lambda_0(x, y):
        return (x + y)
    _lambda_0
    def _lambda_1(*argumentos):
        return print(argumentos)
    _lambda_1
    def _lambda_2(a, b, *resto):
        return (a + b)
    _lambda_2
    a = 1
    b = 2
    (a + b)
    def loop(x):
        return (loop((x - 1)) if (x > 0) else x)
    loop(10)
    x = 1
    y = (x + 1)
    y
    def f():
        return 1
    f()
    x = 1
    y = 2
    (x + y)
    (True and False and (True or False))
    _cond_res_0 = None
    if (x > 10):
        _cond_res_0 = "maior"
    elif (x == 10):
        _cond_res_0 = "igual"
    else:
        _cond_res_0 = "menor"
    _cond_res_0
    _cond_res_1 = None
    if (_test_tmp_0 := testa_algo()):
        _cond_res_1 = processa_resultado(_test_tmp_0)
    _cond_res_1
    i = 0
    j = 10
    while not ((i == 10)):
        print(i)
        print(j)
        _next_i = (i + 1)
        _next_j = (j - 1)
        i = _next_i
        j = _next_j
    j
    (lambda: (10 + 20))
